# 1. Общие замечания к интерфейсу ревизия 4

> **Легенда пометок:**
>
> - <span style="background-color:#fff3cd">желтым</span> — добавлено / уточнено для Codex;
> - <span style="background-color:#f8d7da;color:#842029">красным</span> — вопрос, риск или потенциальное противоречие;
> - <span style="background-color:#d1e7dd;color:#0f5132">зеленым</span> — требование согласуется с текущей архитектурой и предыдущими решениями.

<span style="background-color:#fff3cd">**Контекст проверки:** ревизия 4 должна идти поверх ранее сформированных UI-блоков ревизии 3 и 3.1. Старые задачи не перенумеровывать; новый блок лучше оформить как следующий номер после последнего уже добавленного блока. Онтологию DXF-импорта, профилей, материалов, загружений и консольного command registry не менять.</span>

<span style="background-color:#fff3cd">**Общее правило для Codex:** правки дробить на атомарные UI-задачи: 1) консольные команды БД; 2) DXF dialog toolbar/model tab; 3) DXF profile tab with material selection/search; 4) DXF logs/status taxonomy; 5) wind dialog DTO/UI; 6) project tree/properties panels. После каждой задачи — `cd frontend && npm run build` и `cd frontend && npm run lint`.</span>

### 1.1 Консоль

- Добавить команды базы данных в соответствии с текущей реализацией.
  <span style="background-color:#fff3cd">Уточнение: команды БД добавлять через существующий `command registry`, а не отдельными обработчиками в UI. Команды должны только вызывать уже существующие API/сервисы и показывать результат в консоли/логах.</span>
  <span style="background-color:#f8d7da;color:#842029">Вопрос: нужно явно перечислить команды. По текущему контексту вероятны команды просмотра/обновления справочников профилей и материалов, но без списка есть риск, что Codex реализует не тот набор. Предлагаемый минимум: `db.status`, `db.materials`, `db.profiles`, `db.reload`/`db.refresh` — только если такие backend endpoint уже существуют.</span>
  <span style="background-color:#f8d7da;color:#842029">Риск: не добавлять команды, которые напрямую меняют БД, если для них нет реализованного backend API и сценария подтверждения. На данном этапе безопаснее ограничиться read-only/status/reload-командами.</span>

## 1.2 импорт DXF

- Поле со служебной информацией "Выберите `.dxf` файл, чтобы построить предпросмотр импорта."
  <span style="background-color:#fff3cd">Уточнение: формулировку следует трактовать как требование убрать это поле/служебную подсказку из основного UI. Если файл не выбран, допустимо оставить минимальное пустое состояние внутри вкладки `Модель`, но без отдельного служебного блока.</span>
  <span style="background-color:#f8d7da;color:#842029">Вопрос: здесь, вероятно, пропущено слово “убрать”. Требование противоречит ревизии 3.1, где все лишние пояснения уже убирались. Предлагаю зафиксировать: “Поле со служебной информацией ... убрать”.</span>

- кнопки диагностика и профили перенести в блок кнопок в нижней панели, от остальных кнопок отделить вертикальной чертой, кнопки выполнить с иконками, при переключении на вкладки Профили и Логи кнопки скрывать
  <span style="background-color:#d1e7dd;color:#0f5132">Согласуется с предыдущим решением: режимы отображения относятся к 3D-вкладке `Модель`, поэтому логично показывать их только на этой вкладке.</span>
  <span style="background-color:#fff3cd">Уточнение: нижняя панель должна иметь группы кнопок: `file/import/logs actions` | `view mode` | `rotation/reset`. Разделитель — визуальный `Divider orientation="vertical"` или аналогичный элемент. Для всех icon button обязательны `Tooltip` и `aria-label`.</span>
  <span style="background-color:#f8d7da;color:#842029">Риск: если режимы `Диагностика/Профили` одновременно существуют как вкладки и как кнопки, будет терминологическая путаница. Предлагаю назвать кнопки режима 3D-сцены: `Окраска: диагностика` и `Окраска: профили`, а вкладку оставить `Профили`.</span>

- Поворот вогруг осей X, Y, Z выполнить кнопками с иконками. при нажатии на кнопку осуществляется поворот вокруг соответствующей оси на угол 90 градусов по часовой стрелке. Кнопки перенести в блок кнопок в нижней панели совместно с кнопкой сброса. При переключении на вкладки Профили и Логи кнопки скрывать.
  <span style="background-color:#fff3cd">Опечатка: “вогруг” → “вокруг”.</span>
  <span style="background-color:#fff3cd">Уточнение: поворот должен применяться к preview/import transform, а не к основной модели до подтверждения импорта. После каждого поворота выполнять нормализацию координат preview-модели: `Z >= 0`, модель центрирована по нужной оси/плоскости согласно принятому правилу.</span>
  <span style="background-color:#f8d7da;color:#842029">Вопрос: “по часовой стрелке” для 3D-оси неоднозначно без направления наблюдения. Для реализации лучше задать математически: поворот `+90°` или `-90°` вокруг глобальной оси `X/Y/Z` по правилу правой руки. Предлагаю выбрать `+90°` по правилу правой руки и подписать tooltips: `Повернуть вокруг X на +90°`.</span>
  <span style="background-color:#f8d7da;color:#842029">Риск: кнопка сброса должна сбрасывать только повороты/нормализацию preview, но не сбрасывать выбранные профили, материалы и настройки импорта, если это не указано отдельно.</span>

- Размер 3д сцены - 5:4. Размер сцены и модальной формы выровнять следующим образим: высота формы  без изменений, ширина формы - исходя из ширины 3д сцены. Ширина 3д сцены определяется из соотношения к высоте, высота - заполнение пространства после удаления элементов. Сцену отдельно в рамку не помещать, маржин и паддинг равен нулю.
  <span style="background-color:#fff3cd">Опечатка: “образим” → “образом”.</span>
  <span style="background-color:#fff3cd">Уточнение: соотношение 5:4 применять к canvas/viewport на вкладке `Модель`. Высота берется как доступная высота между шапкой tabs и нижней toolbar-панелью. Ширина модального окна вычисляется от этой высоты: `width = height * 5 / 4` плюс системные отступы/ширина dialog chrome, если они есть.</span>
  <span style="background-color:#f8d7da;color:#842029">Риск: на малых экранах фиксированное 5:4 может выйти за viewport. Нужен responsive fallback: `max-width: calc(100vw - ...)`, `max-height: calc(100vh - ...)`, а при нехватке места сохранять видимость кнопок без горизонтального скролла.</span>
  <span style="background-color:#fff3cd">Уточнение: “маржин и паддинг равен нулю” применять к контейнеру 3D-сцены, а не ко всему диалогу, иначе шапка/tabs/toolbar могут визуально сломаться.</span>

-  Вкладка Профили:

  - Строки сделать больше похожими на табличные
    <span style="background-color:#fff3cd">Уточнение: лучше использовать MUI `Table`/`DataGrid`-подобную разметку или CSS-grid с фиксированными колонками. Визуально — строки высотой 40 px, разделители строк, единое выравнивание полей.</span>

  - Палитра выбора цвета - плитка 4 на 4 (цвета берем из MUI, пример реализации здесь: https://mui.com/material-ui/customization/color/#picking-colors)
    <span style="background-color:#fff3cd">Уточнение: палитра 4×4 должна открываться по клику на цветовой индикатор. Цвет хранить как выбранный UI/display color для группы; исходный DXF color/ACI желательно сохранить в логах для трассировки.</span>
    <span style="background-color:#f8d7da;color:#842029">Вопрос: изменение цвета должно менять только отображение группы в preview или также ключ группировки “цвет → профиль”? Безопаснее считать, что меняется только отображаемый цвет, а связь с исходной DXF-группой сохраняется.</span>

  - Типы профиля - на русском языке
    <span style="background-color:#fff3cd">Уточнение: хранить внутренние ключи типов профиля латиницей/enum как сейчас, отображать через display-name map на русском. Не менять API-контракт ради локализации.</span>

  - Приизменении стандарта менять тип профиля на соответствующий и выбирать соответствующий профиль (первый из списка)
    <span style="background-color:#fff3cd">Опечатка: “Приизменении” → “При изменении”.</span>
    <span style="background-color:#fff3cd">Уточнение: порядок зависимости должен быть: `Стандарт` → доступные `Типы профиля` → доступные `Профили` → доступные `Материалы` по толщине/профилю. При смене стандарта выбрать первый доступный тип и первый профиль только если текущие значения стали невалидными.</span>
    <span style="background-color:#f8d7da;color:#842029">Риск: “первый из списка” должен быть детерминированным. Нужна сортировка по display name/размеру, иначе выбор будет нестабилен при изменении порядка данных API.</span>

  - Надписи "Назначьте профиль для этой группы." - убираем
    <span style="background-color:#d1e7dd;color:#0f5132">Согласуется с общей очисткой UI от пояснений. Ошибку неназначенного профиля лучше показывать статусом строки/валидацией, а подробность — в логах.</span>

  - Добавить выбор материала, при смене профиля менять материал соответствующим образом (в зависимости от толщины - первый из списка)
    <span style="background-color:#d1e7dd;color:#0f5132">Согласуется с текущей моделью: профиль выбирается первым, материалы фильтруются по толщине профиля.</span>
    <span style="background-color:#fff3cd">Уточнение: материал должен храниться как `material_id`, отображение — русское название/марка. При смене профиля материал автоматически менять только если текущий материал не подходит по толщине/доступности; если подходит — сохранять пользовательский выбор.</span>
    <span style="background-color:#f8d7da;color:#842029">Вопрос: если для толщины профиля нет подходящих материалов, строка должна стать ошибочной и импорт должен блокироваться, либо разрешать импорт с пустым материалом? Для расчетной модели логичнее блокировать импорт до назначения материала.</span>

  - В поле Профиль добавить возможность поиска
    <span style="background-color:#fff3cd">Уточнение: использовать autocomplete/searchable select, фильтр по designation/display_name/standard/profile_type. Поиск должен работать внутри уже выбранного стандарта и типа профиля.</span>

  - В целом сделать более похожим на таблицу, все поля (кромк поля Цвет) выровнять по ширине
    <span style="background-color:#fff3cd">Опечатка: “кромк” → “кроме”.</span>
    <span style="background-color:#fff3cd">Уточнение: рекомендуемая структура колонок: `Цвет` — фиксированная ширина 56–72 px; `Тип профиля`, `Стандарт`, `Профиль`, `Материал` — равные/адаптивные ширины, единая высота controls 40 px.</span>

- Вкладка Логи:

  - Не понятен смысл предупреждения INE #19

    DXF handle: 7FD / Слой: 0 / Группа: ACI_2 / Исходный цвет: #ffff00
    <span style="background-color:#fff3cd">Уточнение: код `INE` должен быть расшифрован в пользовательском тексте. Если это `ignored entity`, выводить понятную строку: `Статус: предупреждение. Элемент DXF проигнорирован: тип/причина ...`. Один handle — одна агрегированная запись.</span>
    <span style="background-color:#f8d7da;color:#842029">Вопрос: если элемент является валидной LINE и участвует в модели, такое предупреждение ошибочно и должно быть убрано. Если это не LINE/неподдерживаемый объект — предупреждение допустимо, но нужно показывать тип объекта и причину игнорирования.</span>

  - Из текста убрать чипы (заминить на текст - Статус: ...)
    <span style="background-color:#fff3cd">Опечатка: “заминить” → “заменить”.</span>
    <span style="background-color:#d1e7dd;color:#0f5132">Согласуется с требованием сделать логи компактными и менее визуально шумными.</span>

  - Из панели кнопок убрать дублирующую кнопку Логи импорта
    <span style="background-color:#fff3cd">Уточнение: если логи теперь доступны как вкладка `Логи`, отдельная кнопка открытия логов в нижней панели не нужна. Исключение — если кнопка используется для перехода на вкладку `Логи`; тогда она должна быть не дублирующей, а явным tab switch. Но предпочтительно убрать.</span>

- Дополнительно: Импортирую полностью исправную модель - она желтая. мне не понятно почему, возможно излишняя проверка, нужно согласовать список ошибок и предупреждений импорта
  <span style="background-color:#f8d7da;color:#842029">Критичное замечание: нужна формальная таксономия статусов импорта. Без нее Codex будет продолжать “желтить” модель по несущественным предупреждениям. Предлагаю разделить: `error` — импорт блокируется; `warning` — импорт разрешен, но есть инженерно значимое отклонение; `info` — служебная информация, не влияет на цвет модели.</span>
  <span style="background-color:#fff3cd">Предлагаемый минимум: `error` — нулевая длина LINE, не назначен профиль/материал, невалидные координаты, невозможность построить узлы/стержни; `warning` — висячие стержни, несколько несвязанных компонент, Z-диапазон был нормализован, unsupported entity ignored при наличии таких объектов; `info` — количество LINE, слои, цвета, DXF handle, параметры импорта.</span>
  <span style="background-color:#f8d7da;color:#842029">Вопрос на согласование: считать ли `unsupported entity ignored` предупреждением или информацией. Для DXF-импорта LINE-only логичнее: если в файле есть лишние объекты, но все LINE импортированы корректно, это `info`, чтобы исправная стержневая модель не становилась желтой.</span>

## 1.3 Диалог ввода параметров ветра

- Перевести Assembly load case
  <span style="background-color:#fff3cd">Уточнение: заменить пользовательский текст `Assembly load case` на русский вариант. Рекомендуемое наименование: `Загружение сборки` или просто `Загружение`, если контекст уже ясен.</span>

- нет отступа от верхнего выпадающего списка до шапки
  <span style="background-color:#fff3cd">Уточнение: выровнять через единый `Stack spacing`; не добавлять точечные `marginTop` на отдельное поле без общей сетки формы.</span>

- высота полей - 40 пиксел
  <span style="background-color:#fff3cd">Уточнение: применить ко всем controls формы ветра: select, number fields, autocomplete/inputs, icon buttons — визуально 40 px.</span>

- Доработать DTO и добавить параметры: Тип местности ["A", "B", "C"], коэффициент надежности (1.4 по умолчанию), режим расчета [Простой, СП 20.13330, ПУЭ 7-е изд.]
  <span style="background-color:#fff3cd">Уточнение: UI-значения русские, внутренние DTO enum — стабильные латинские ключи: `simple`, `sp20`, `pue`. `terrainType`: `A | B | C`. `gammaF` по умолчанию 1.4.</span>
  <span style="background-color:#f8d7da;color:#842029">Риск: коэффициент надежности 1.4 корректен для режима СП, но для ПУЭ может требоваться отдельный нормативный коэффициент/логика. Нельзя автоматически перемножать коэффициенты СП и ПУЭ. В UI нужно показывать один применяемый коэффициент для выбранного режима.</span>
  <span style="background-color:#f8d7da;color:#842029">Вопрос: поле давления в интерфейсе уже ранее уточнялось как `Па`, не `кПа`. В этой задаче нужно явно закрепить единицу отображения и DTO-конвертацию, чтобы не вернулась путаница с `nominalPressureKPa`.</span>

## 1.4 Панель Состав проекта

- После вкладок нагрузки и элементы если они в развернутом состоянии рисовать разделительную линию на всю ширину меню
  <span style="background-color:#fff3cd">Уточнение: разделитель показывать только после раскрытого содержимого соответствующей секции, на всю ширину панели, без нарушения внутренних отступов строк.</span>
  <span style="background-color:#f8d7da;color:#842029">Вопрос: формулировка “после вкладок нагрузки и элементы” вероятно означает секции/группы `Нагрузки` и `Элементы` в панели состава проекта, а не tabs. Предлагаю переименовать в тексте задачи: “после раскрытых секций `Нагрузки` и `Элементы`”.</span>

- Высота всех элементов - 40 пиксел
  <span style="background-color:#fff3cd">Уточнение: применить к строкам дерева/списка, кнопкам действий и элементам секций панели. Для многострочных названий — обрезка с tooltip, без увеличения высоты строки.</span>

## 1.5 Панель свойства

- В заголовке убрать вторую строку
  <span style="background-color:#fff3cd">Уточнение: оставить только основную строку заголовка выбранного объекта/состояния; вторичную информацию перенести в свойства таблицы или tooltip, если она нужна.</span>

- Свойства выполнить ввиде таблицы по образу свойств Autocad
  <span style="background-color:#fff3cd">Опечатка: “ввиде” → “в виде”.</span>
  <span style="background-color:#fff3cd">Уточнение: структура таблицы AutoCAD-like: левая колонка — наименование свойства, правая — значение/редактор. Группы свойств можно отделять компактными заголовками секций. Редактируемые значения должны сохранять текущие механизмы валидации и обновления модели.</span>
  <span style="background-color:#f8d7da;color:#842029">Риск: при переводе панели свойств на таблицу нельзя потерять существующие действия/редакторы для выбранных сущностей. Нужно отдельно проверить: узел, стержень, загружение, сила/момент, закрепление/DOF, профиль/материал, отсутствие выбора.</span>

- Высота всех элементов - 40 пиксел
  <span style="background-color:#fff3cd">Уточнение: применить к строкам таблицы свойств и редакторам значений. Для длинных значений использовать обрезку/tooltip или перенос только в специально разрешенных многострочных полях.</span>
