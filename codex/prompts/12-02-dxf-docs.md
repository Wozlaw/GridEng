# Task 12.2 — DXF import specification documentation

## Задача
Создай документацию `frontend/docs/dxf-import-spec-v0.1.md`.

## Описать
- поддерживается только `LINE`;
- используются coordinates, `color`, `colorIndex`, `trueColor`, `layer`, `handle`;
- остальные primitives игнорируются, перечислить их явно;
- tolerance соединения линий;
- проверка 2D/3D;
- центрирование по XY;
- Z вверх;
- color/layer groups;
- проверка висячих стержней;
- preview импорта;
- errors/warnings;
- ограничения импорта.

Не менять код.
