# Task 4.1 — MUI ThemeProvider

## Задача
Добавь MUI provider в `frontend/src/app/providers/AppProviders.tsx`.

## Требования
- `ThemeProvider`.
- `CssBaseline`.
- Темная CAD-like тема.
- Компактные controls.
- `App.tsx` должен использовать `AppProviders`.

## Проверки
- `cd frontend && npm run build`
- `cd frontend && npm run lint`
