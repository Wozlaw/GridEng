# Общие правила выполнения

- Работай маленьким diff. Не выполняй соседние задачи из следующих файлов.
- Перед правками дай краткий план и перечисли файлы, которые планируешь менять.
- Проект уже выполнен до задачи 22.6 включительно. Не откатывай реализованные задачи 10.1–22.6; работай поверх текущего состояния.
- Сохраняй текущую онтологию проекта: `CadShell`, `TopMenu`, `ProjectTreePanel`, `PropertiesPanel`, `Viewport3D`, `CommandConsole`, `modelStore`, `APP_COMMANDS_BY_ID`, `RIBBON_COMMAND_GROUPS`, `runAppCommand`, `DxfImportPreview`, `DxfImportSettings`, `ImportLogDialog`/`ImportLogDrawer`.
- Все новые пользовательские действия заводи через существующий `command registry`; не создавай параллельные локальные обработчики бизнес-логики.
- Frontend: `frontend/`, React + TypeScript + Vite + MUI + Three/R3F + Zustand.
- Backend, расчетное ядро, БД и API сечений не трогать.
- Сохраняй black & white / grayscale эстетику. Цвет в DXF preview и profile mapping — функциональная диагностическая окраска, а не декоративная цветная тема.
- Все подробные данные, служебную информацию, счетчики, JSON/debug и параметры импорта показывать только во вкладке/окне логов. Основной UI должен быть чистым.
- После каждой задачи запусти: `cd frontend && npm run build` и `cd frontend && npm run lint`.
- В конце ответа покажи: summary, измененные файлы, команды, результат build/lint, риски/техдолг.

Источник требований: `codex/reference/UI_REVISION_3_1_REQUIREMENTS.md`.

# Task 23.5 — Wind dialog final cleanup and UI revision 3.1 docs

## Цель

Выполнить финальную очистку диалога `Ветер` и закрыть ревизию 3.1 документацией/checklist.

## Требования к диалогу `Ветер`

1. Убрать подпись `Загружение` над выпадающим списком.
2. Сам выпадающий список оставить; в нем по-прежнему показывать только наименования загружений.
3. Внутреннее значение select должно оставаться `loadCase.id`.
4. Убрать информационное поле/информационный alert из формы.
5. Не возвращать поле `Z`; направление ветра в UI остается `X/Y`, а при сохранении `direction.z = 0`.
6. Не менять модель ветра и API: 3D-вектор сохраняется, UI только ограничивает ввод текущей итерации.
7. Сохранить валидацию давления в Па:
   - отрицательные значения запрещены;
   - `0` Па допустимо;
   - шаг давления `10` Па.
8. Сохранить icon button с tooltip/aria-label.

## Документация UI revision 3.1

Добавить или обновить `codex/docs/ui-revision-3-1-implementation-notes.md`.

Зафиксировать:

1. новую структуру DXF import dialog: шапка → tabs `Модель | Профили | Логи` → нижний блок кнопок;
2. правило: вся служебная информация только в `Логи`;
3. preview-нормализацию: `Z >= 0`, ось `Z` по центру модели;
4. поворот preview вокруг осей и reset;
5. структуру таблицы профилей `Цвет | Тип профиля | Стандарт | Профиль`;
6. правило цветового индикатора и палитры;
7. компактные агрегированные логи без JSON;
8. финальные правки диалога `Ветер`.

## Ручной checklist ревизии 3.1

Проверить:

1. DXF dialog открывается с tabs `Модель | Профили | Логи`.
2. На вкладке `Модель` 3D preview занимает доступное пространство.
3. Preview после загрузки нормализован: `Z >= 0`, модель центрирована относительно оси `Z`.
4. Поворот вокруг осей работает и имеет reset.
5. На вкладке `Профили` таблица имеет только колонки `Цвет | Тип профиля | Стандарт | Профиль`.
6. Цветовой индикатор открывает палитру.
7. Счетчики и служебная информация не показываются на вкладке `Профили`.
8. Вкладка `Логи` компактная, без raw JSON, сообщения сгруппированы.
9. Кнопка `Логи` активирует вкладку логов или открывает переиспользуемый лог-view без дублирования.
10. Диалог `Ветер` без подписи `Загружение` и без информационного поля.
11. Build/lint проходят.

## Ограничения

- Не добавлять новые UX-концепции вне ревизии 3.1.
- Не менять расчетное ядро и backend.
- Не переименовывать поля модели.

## Критерии готовности

- Диалог `Ветер` очищен согласно требованиям.
- Документация ревизии 3.1 добавлена.
- Checklist зафиксирован.
- Build/lint проходят.

## Проверка

```bash
cd frontend
npm run build
npm run lint
```
