# Task 9.2 — Restraint markers rendering

## Задача
Добавь визуализацию restraints в `frontend/src/widgets/viewport-3d/RestraintMarkers.tsx`.

## Требования
- Показывать markers на узлах с `restraint`.
- Различать полное защемление и частичное закрепление.
- Учитывать `visibility.restraints`.
- Не перегружать сцену сложными meshes.

## Проверки
- `cd frontend && npm run build`
- `cd frontend && npm run lint`
