# Общие правила выполнения

- Работай маленьким diff. Не выполняй соседние задачи из следующих файлов.
- Перед правками дай краткий план и перечисли файлы, которые планируешь менять.
- Проект уже выполнен до задачи 20.2 включительно. Не откатывай реализованные задачи 10.1–20.2; работай поверх текущего состояния.
- Сохраняй текущую онтологию проекта: `CadShell`, `TopMenu`, `ProjectTreePanel`, `PropertiesPanel`, `Viewport3D`, `CommandConsole`, `modelStore`, `APP_COMMANDS_BY_ID`, `RIBBON_COMMAND_GROUPS`, `runAppCommand`.
- Frontend: `frontend/`, React + TypeScript + Vite + MUI + Three/R3F + Zustand.
- Backend, расчетное ядро и БД не трогать. Нормативный расчет ветра не реализовывать.
- Все пользовательские сообщения/заглушки показывать через единый MUI Alert/Snackbar снизу по центру.
- После каждой задачи запусти: `cd frontend && npm run build` и `cd frontend && npm run lint`.
- В конце ответа покажи: summary, измененные файлы, команды, результат build/lint, риски/техдолг.

Источник требований: `codex/reference/UI_REVISION_2_CHECKED_REQUIREMENTS.md`.

# Task 21.8 — Заглушки, финальная интеграционная проверка и документация UI revision 2

## Цель

Закрыть интерфейсную ревизию 2: проверить все новые команды/заглушки, обновить документацию и зафиксировать ручной checklist регрессий.

## Placeholder-команды

Убедиться, что следующие команды существуют и показывают MUI Alert/Snackbar info, если реальная функциональность еще не реализована:

- `Открыть`;
- `Сохранить`;
- `Экспорт DXF`;
- `Каталог конструкций`;
- `Каталог профилей`;
- `Каталог материалов`;
- `Сводка`, если нет полноценной аналитики;
- `Отчет`;
- `Спецификация`;
- `Справка`;
- `О программе`;
- `Лицензия`.

Существующие заглушки не дублировать: привести их к единому стилю сообщений.

## Документация

Обновить или добавить краткий документ, например:

- `codex/docs/ui-revision-2-implementation-notes.md`;
- или существующий подходящий файл в `codex/docs/`.

В документе зафиксировать:

1. новую структуру `TopMenu`;
2. назначение `activeLoadCaseId`;
3. назначение `visibility.labels`;
4. расположение overlay-элементов сцены;
5. новую модель docked/fullscreen console;
6. ручной checklist проверки.

## Ручной checklist

Проверить сценарии:

1. запуск приложения;
2. импорт JSON;
3. импорт DXF;
4. экспорт JSON;
5. переключение загружения;
6. видимость нагрузок/закреплений/надписей;
7. режимы `Каркас` / `Реальный` / `Карта напряжений`;
8. wind overlay активного загружения;
9. stress legend слева снизу;
10. gizmo справа снизу;
11. docked-консоль;
12. fullscreen-консоль;
13. ширина 1366 px без горизонтального overflow;
14. открытие всех placeholder-команд без ошибок.

## Ограничения

- Не добавлять новую крупную функциональность в финальной задаче.
- Не менять backend.
- Не менять модель данных без отдельной причины.

## Критерии готовности

- Все команды из новой структуры меню существуют.
- Все нереализованные функции дают единый Alert.
- Документация UI revision 2 обновлена.
- Build/lint проходят.

## Проверка

```bash
cd frontend
npm run build
npm run lint
```
