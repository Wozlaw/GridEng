# Task 6.3 — Fit to model

## Задача
Добавь функцию fit-to-model в viewport.

## Требования
- Bounding box по `nodes`.
- Кнопка `Fit` в `TopMenu` или viewport overlay.
- Камера перемещается так, чтобы вся модель была видна.
- Если модель пустая — ничего не делать.

## Проверки
- `cd frontend && npm run build`
- `cd frontend && npm run lint`
